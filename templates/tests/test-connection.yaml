apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jbok.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "jbok.name" . }}
    helm.sh/chart: {{ include "jbok.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jbok.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
